function makeShape(){

		//for number of rows entered in enteredNum
		for(var numLines = 0; numLines <= enteredNum - 1; numLines++ ){	
			//for number of spaces in numberOfSpacesFirstLine
			for(var numSpaces = numberOfSpacesFirstLine; numSpaces <= numberOfSpacesFirstLine + 1; numSpaces--){
				$(".shape").append('\u0020');//add spaces to left
			}
			
			//add stars
		    for(var numStars = 0; numStars <= enteredNum - 1; numStars++){
				$(".shape").append("*");
			}

		}
		
		//for number of rows entered in enteredNum
		/*for(var numLines = 0; numLines <= enteredNum - 1; numLines++ ){		
			//now add spaces asc
			for(var numSpaces = 0; numSpaces <= numberOfSpacesFirstLine + 1; numSpaces++ ){
				$(".shape").append(" ");//add spaces to left
			
				//add stars
				for(var numStars = 0; numStars <= enteredNum - 1; numStars++){
					$(".shape").append("*");
				}
				
			}
		}
	     */
	}